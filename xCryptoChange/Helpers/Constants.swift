import Foundation

// MARK: - Font Size Constants
enum FontSize {
    static let large    : CGFloat = 16.0
    static let `default`: CGFloat = 14.0
    static let minimum  : CGFloat = 10.0
}

// MARK: - Margin Constants
enum Margin {
    static let largest  : CGFloat = 18.0
    static let large    : CGFloat = 14.0
    static let `default`: CGFloat = 10.0
}

// MARK: - Sizing {
enum Sizing {
    static let rowHeight: CGFloat = 72.0
}

// MARK: - Spacing Constants
enum Spacing {
    static let extraLarge: CGFloat = 30.0
    static let large     : CGFloat = 12.0
    static let `default` : CGFloat = 8.0
    static let mininum   : CGFloat = 2.0
}
